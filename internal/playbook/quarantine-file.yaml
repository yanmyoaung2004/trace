name: quarantine-file
description: Move a suspicious file to quarantine
triggers:
  - quarantine
  - isolate
  - quarantine file
steps:
  - agent: response
    action: quarantine_file
    params:
      path: ${input.path}
