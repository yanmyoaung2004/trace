name: kill-process
description: Terminate a suspicious process
triggers:
  - kill
  - terminate
  - stop process
steps:
  - agent: response
    action: kill_process
    params:
      name: ${input.name}
