# MINIO_ROOT_USER / MINIO_ROOT_PASSWORD are required (compose errors
# without them — no committed defaults). TRACE_API_KEY_FILE points at a
# host file holding the agent enrollment key (0600).
MINIO_ROOT_USER=
MINIO_ROOT_PASSWORD=
TRACE_API_KEY_FILE=./secrets/agent.key
